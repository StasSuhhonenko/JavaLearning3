package com.rHopliteProd.VehicleProject;

public class Car extends Vehicle {
    public Car(String roamingEnvironment, String engineType, int numberOfDoors, int fuelType, int numberOfWheels, String coveringDistance, int numberOfSeats) {
        super(roamingEnvironment, engineType, numberOfDoors, fuelType, numberOfWheels, coveringDistance, numberOfSeats);
    }
}
